# Documentation

Files under `docs/` are educational and operational reference material for humans.

Hermes should not treat these documents as executable instructions by default. A docs file only becomes part of execution when a Hermes prompt, skill, automation blueprint, or script explicitly tells the agent to read and follow that specific file.

Execution behavior should live in:

- `skills/`
- `automations/`
- `scripts/`

